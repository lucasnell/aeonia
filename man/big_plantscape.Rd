% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plantscape-big.R
\name{big_plantscape}
\alias{big_plantscape}
\title{Simulate large (100 x 100) plantscape.}
\usage{
big_plantscape(
  n_sims,
  landscape,
  pseudo_repel,
  pseudo_surv,
  zeta,
  N0,
  sd_N,
  Y0,
  W0 = 0,
  sd_W = 0,
  radius = pop_info$radius,
  virus_attract = 1,
  fly_p = 0.05,
  p_load_alate = 0.5,
  p_load_plant = 0.5,
  ...
)
}
\arguments{
\item{landscape}{Integer matrix of size 100 x 100 with the types of each
plant in each landscape. It's assumed that rows are x the dimension and
columns are the y dimension.
Values in each cell give the state of the plant:
\code{0} indicates nothing on plant,
\code{1} indicates just virus on plant (infectious),
\code{2} indicates just \emph{Pseudomonas} on plant,
\code{3} indicates both virus and \emph{Pseudomonas} on plant.
Values < 0 or > 3 are not allowed.}

\item{pseudo_repel}{Effect of \emph{Pseudomonas} infection on alate alighting.
Sampling weights for \emph{Pseudomonas}-containing plants is
\code{1 / pseudo_repel} (note difference from \code{virus_attract}, hence the
different names!), compared to empty (no virus or \emph{Pseudomonas})
plants whose weight is 1.
Thus, when \code{pseudo_repel > 1}, alates are repelled by
\emph{Pseudomonas}-containing plants,
while \code{pseudo_repel < 1} causes them to be attracted to
\emph{Pseudomonas}-containing plants.
Values must be \verb{> 0}.}

\item{pseudo_surv}{Single numeric indicating aphid survival from \emph{Pseudomonas}.}

\item{zeta}{Constant between 0 and 1 that affects the extent to which
parasitoids respond to aphid density, where 0 results in an even
distribution of parasitoids, and 1 results in a linear relationship
between aphid density and parasitoids.}

\item{N0}{If a single numeric, it gives the mean for the lognormal
distribution used to generate non-winged aphid densities per patch.
If a numeric matrix, it must be 100 x 100 and gives the abundance of non-winged
aphids per plant that is the same across repetitions.
If a 3D array, it must be 100 x 100 x \code{n_sims} and gives the abundance of
non-winged aphids per plant across repetitions.}

\item{Y0}{Numeric vector indicating the starting parasitoid population
density for each rep.
The vector can also be of length 1, in which case it's assumed that
all reps start with the same density of parasitoids.}

\item{W0}{If a single numeric, it gives the mean for the lognormal
distribution used to generate winged aphid densities per patch.
If a numeric matrix, it must be 100 x 100 and gives the abundance of winged
aphids per plant that is the same across repetitions.
If a 3D array, it must be 100 x 100 x \code{n_sims} and gives the abundance of
winged aphids per plant across repetitions.
Defaults to \code{0}.}

\item{radius}{Max distance that alates will travel between plants.
Must be >= 1.
See "Radius" section in the description for \code{\link[=lil_plantscape]{lil_plantscape()}} for details.
Defaults to \code{pop_info$radius}.}

\item{virus_attract}{Effect of virus infection on alate alighting.
Sampling weights for virus-infectious plants is \code{virus_attract},
compared to empty (no virus or \emph{Pseudomonas}) plants whose weight is 1.
Thus, when \code{virus_attract > 1}, alates are attracted to virus-infectious
plants, while \code{virus_attract < 1} causes them to be repelled by
virus-infectious plants.
Values must be \verb{> 0}.}

\item{fly_p}{Single numeric indicating the proportion of alates that fly
off plants each day.}

\item{p_load_alate}{Single numeric indicating the probability that an
uninoculated alate is loaded with a virus if it interacts with an
inoculated plant.}

\item{p_load_plant}{Single numeric indicating the probability that an
uninoculated plant is loaded with a virus if it interacts with an
inoculated alate.}

\item{...}{Other parameters for functions \code{\link[=sim_plantscape]{sim_plantscape()}},
\code{\link[=make_disease_ptr]{make_disease_ptr()}}, or \code{\link[=make_insect_ptr]{make_insect_ptr()}}.}
}
\value{
A tibble with columns following the description in the
"Summarizing" section in the docs for function \code{\link[=sim_plantscape]{sim_plantscape()}}.
}
\description{
Simulate large (100 x 100) plantscape.
}
