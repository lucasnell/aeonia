% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{col_namer}
\alias{col_namer}
\title{Just create column names, for use in R.}
\usage{
col_namer(summ, out_pseudo, out_attack_surv, out_stages, n_age_stages = 29L)
}
\arguments{
\item{summ}{Single string to indicate how to summarize output.
If \code{summ == "none"}, then no summarizing is done, so output is separate
by plant.
If \code{summ == "time"}, then output is summarized across all plants, so
will only be separated by time and rep.
If \code{summ == "all"}, then output is summarized across all plants and
time points, so will only be separated by rep.
See "Summarizing" section below for details on output columns.
Defaults to \code{"none"}.}

\item{out_pseudo}{Single logical for whether to include \emph{Pseudomonas}
presence in output. Ignored if \code{summ != "none"}.
Defaults to \code{FALSE}.}

\item{out_attack_surv}{Single logical for whether to include aphid survival
from parasitoid attack (weighted mean by abundance for each aphid stage)
in output. Ignored if \code{summ != "none"}.
Defaults to \code{FALSE}.}

\item{out_stages}{Single string for whether and how to separate output for
aphids stage.
If \code{"none"}, no stages are output.
If \code{"two"}, adult and juvenile alates and apterous aphids are separated.
If \code{"all"}, every age stage is separated, typically \code{29} each for
aphids and alates.
Defaults to \code{"none"}.}
}
\description{
Just create column names, for use in R.
}
