% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aphid-wasp-classes.R
\name{make_insects_ptr}
\alias{make_insects_ptr}
\title{Create a list in which to store insect population info.}
\usage{
make_insects_ptr(
  pseudo_surv,
  zeta,
  extinct_N = 0,
  demog_error = FALSE,
  environ_error = FALSE,
  K = an_populations$K,
  K_p_mult = an_populations$K_p_mult,
  pred_surv = 0.9,
  alate_b0 = -5,
  alate_b1 = 0.0022,
  fly_p = 0.05,
  distr_0 = NULL,
  resistant = FALSE,
  surv_juv_apterous = NULL,
  surv_adult_apterous = NULL,
  repro_apterous = NULL,
  surv_juv_alates = "low",
  surv_adult_alates = "low",
  repro_alates = "low",
  surv_paras = "low",
  temp = "low",
  p_instar_smooth = 0.5,
  sigma_x = an_environ$sigma_x,
  rho = an_environ$rho,
  s_y = an_populations$s_y,
  sex_ratio = an_populations$sex_ratio,
  a = an_wasp_attack$a,
  k = an_wasp_attack$k,
  h = an_wasp_attack$h,
  rel_attack = an_wasp_attack$rel_attack,
  sigma_y = an_environ$sigma_y,
  mummy_smooth = 0.4
)
}
\arguments{
\item{pseudo_surv}{Single numeric indicating aphid survival from \emph{Pseudomonas}.}

\item{zeta}{Constant between 0 and 1 that affects the extent to which
parasitoids respond to aphid density, where 0 results in an even
distribution of parasitoids, and 1 results in a linear relationship
between aphid density and parasitoids.}

\item{extinct_N}{Single numeric indicating the extinction threshold.
Defaults to \code{0}.}

\item{demog_error}{Single logical for whether to include demographic
stochasticity. Defaults to \code{FALSE}.}

\item{environ_error}{Single logical for whether to include environmental
stochasticity. Defaults to \code{FALSE}.}

\item{K}{A single number indicating aphid density dependence.
Defaults to \code{12.5e3} because this caused simulations to
approximately match experiments.}

\item{K_p_mult}{A single number that is multiplied by \code{K} to get density
dependence for parasitized aphids.
Defaults to \code{1 / 1.57}, which is from Meisner et al. (2014).}

\item{pred_surv}{Daily survival from predation for aphids and mummies.
Defaults to \code{0.9}.}

\item{alate_b0}{The proportion of offspring from apterous aphids is
\code{inv_logit(alate_b0 + alate_b1 * N)} where \code{N} is the total number of
aphids in that field.
Defaults to \code{-5}.}

\item{alate_b1}{The proportion of offspring from apterous aphids is
\code{inv_logit(alate_b0 + alate_b1 * N)} where \code{N} is the total number of
aphids in that field.
Defaults to \code{0.0022}, which makes alate production only mildly
density dependent.}

\item{fly_p}{Single numeric indicating the proportion of alates that fly
off plants each day. Defaults to \code{0.05}.}

\item{distr_0}{A 2-column matrix indicating the starting stage distribution.
(The total abundance of aphids is adjusted in the simulation function.)
If \code{NULL}, there will be no starting alates and apterous densities
will follow the stable age distribution.
If a matrix, it must have 5 rows (rows indicate instar)
or a row per aphid age (rows indicate age in days).
Matrix column indicates apterous vs alate.
The matrix will be coerced to sum to 1.
Defaults to \code{NULL}.}

\item{resistant}{Logical or vector of survivals of
singly attacked and multiply attacked aphids.
If a logical, \code{FALSE} is equivalent to \code{c(0,0)} and results in no
resistance.
\code{TRUE} results in the resistance values for a resistance line
from unpublished work by Anthony Ives.
Defaults to \code{FALSE}.}

\item{surv_juv_apterous}{A single number for the juvenile survival rate for
apterous aphids. Defaults to \code{NULL}, which results in estimates
from a medium-reproduction line.}

\item{surv_adult_apterous}{A vector of adult survival probabilities for
apterous aphids. Defaults to \code{NULL}, which results in estimates
from a medium-reproduction line.}

\item{repro_apterous}{A vector of fecundities for for apterous aphids.
Defaults to \code{NULL}, which results in estimates from a
medium-reproduction line.}

\item{surv_juv_alates}{A single number for the juvenile survival rate for
alates aphids. Defaults to \code{"low"}, which results in estimates
from a low-reproduction line.}

\item{surv_adult_alates}{A vector of adult survival probabilities for
alates aphids. Defaults to \code{"low"}, which results in estimates
from a low-reproduction line.}

\item{repro_alates}{A vector of fecundities for for alates aphids.
Defaults to \code{"low"}, which results in estimates from a low-reproduction
line.}

\item{surv_paras}{A single number for the juvenile survival rate for
parasitized aphids.
Because parasitized aphids don't make it to adulthood, this is the only
survival rate necessary.
Defaults to \code{"low"}, which results in estimates from a
low-reproduction line.}

\item{temp}{Single string specifying \code{"low"} (20º C) or \code{"high"} (27º C)
temperature. Defaults to \code{"low"}.}

\item{p_instar_smooth}{A value greater than zero here makes not all 4th
instar aphids go to the adult stage at the same time.
Of the 4th instars that would've moved to adulthood, \code{p_instar_smooth}
remain as 4th instars.
To keep the growth rate approximately equal, for the aphids that
would've transitioned to 4th instar, \code{p_instar_smooth} move to
adulthood instead.
Defaults to \code{0.5}.}

\item{sigma_x}{Standard deviation of environmental stochasticity for aphids.
This argument has no effect (is changed to \code{0}) when
\code{environ_error = FALSE}.
Defaults to the internal object \code{an_environ$sigma_x},
which is from Meisner et al. (2014).}

\item{rho}{Environmental correlation among instars.
This argument has no effect if used in simulations with no environmental
error (i.e., when \code{environ_error = FALSE}).
Defaults to \code{an_environ$rho}.}

\item{s_y}{Daily survival rate of adult wasps.
Defaults to \code{an_populations$s_y}, which is from Meisner et al. (2014).}

\item{sex_ratio}{Sex ratio of adult wasps. Defaults to \code{0.5}.}

\item{a}{Parasitoid attack rate. Defaults to the internal object
\code{an_wasp_attack$a}, which is from Meisner et al. (2014).}

\item{k}{Aggregation parameter of the negative binomial distribution.
Defaults to the internal object \code{an_wasp_attack$k},
which is from Meisner et al. (2014).}

\item{h}{Parasitoid handling time. Defaults to the internal object
\code{an_wasp_attack$h}, which is from Meisner et al. (2014).}

\item{rel_attack}{Relative parasitoid attack rate among instars.
Defaults to \code{an_wasp_attack$rel_attack}, which is from
Meisner et al. (2014).}

\item{sigma_y}{Standard deviation of environmental stochasticity for wasps.
This argument has no effect (is changed to \code{0}) if
\code{environ_error = FALSE}.
Defaults to the internal object \code{an_environ$sigma_y},
which is from Meisner et al. (2014).}

\item{mum_smooth}{Proportion of mummies that will NOT take exactly 3 days
to develop. As this value approaches 2/3, it will provide greater
smoothing of wasp numbers through time.
Defaults to \code{0.4}.}
}
\value{
An \code{externalptr} object that points to a C++ object that can
be passed to \code{\link[=sim_plantscape]{sim_plantscape()}}.
}
\description{
This object is used as an argument to \code{\link[=sim_plantscape]{sim_plantscape()}}.
}
\details{
To smooth aphid stage structure out over time,
not all 4th instar aphids move to adulthood immediately.
If adulthood starts on day \code{t}, then half of aphids at age \code{t-2} move to
adulthood, and half at age \code{t-1} do, too.
I adjusted age \code{t-2}, too, to avoid this affecting the growth rate too much.
}
