% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{sim_plantscape}
\alias{sim_plantscape}
\title{Simulation plant landscapes with virus spread.}
\usage{
sim_plantscape(
  landscapes,
  max_t,
  insect_ptr,
  N0,
  W0,
  Y0,
  virus_attract,
  pseudo_repel,
  epsilon,
  p_load_alate,
  p_load_plant,
  total_exp_days = 7L,
  w = 0.2,
  radius = 7.336451,
  wasp_plant_attract = NULL,
  summ = "none",
  infect_time_n = 0L,
  infect_stop = TRUE,
  out_pseudo = FALSE,
  show_progress = FALSE,
  n_threads = 0L
)
}
\arguments{
\item{landscapes}{Integer cube with the types of each plant in each
landscape. It's assumed that rows are x the dimension,
columns are the y dimension, and "slices" (i.e., \code{dim(landscapes)[3]})
indicate separate landscapes.
Values in each cell give the state of the plant:
\code{0} indicates nothing on plant,
\code{1} indicates just virus on plant (infectious),
\code{2} indicates just \emph{Pseudomonas} on plant,
\code{3} indicates both virus and \emph{Pseudomonas} on plant.
Values < 0 or > 3 are not allowed.
Note that this array is coerced to an array of unsigned integers, so
negative values will become very large integers.
Hence, do not expect an error for negative numbers if you pass them here.}

\item{max_t}{Single integer giving the maximum time the simulations run.}

\item{insect_ptr}{External pointer to a C++ object with insect population
information, output from function \code{\link[=make_insect_ptr]{make_insect_ptr()}}.}

\item{N0}{Numeric 3D array indicating the starting aphid (non-winged) population
density for each plant and rep.
To indicate separate densities for each plant and/or rep,
the array should have the same dimensions as \code{landscapes}.
The array can also be 1x1, in which case it's assumed that all plants
and reps start with the same density of aphids.}

\item{W0}{Numeric 3D array indicating the starting winged aphid population
density for each plant and rep.
To indicate separate densities for each plant and/or rep,
the array should have the same dimensions as \code{landscapes}.
The array can also be 1x1, in which case it's assumed that all plants
and reps start with the same density of winged aphids.}

\item{Y0}{Numeric vector indicating the starting parasitoid population
density for each rep.
The vector can also be of length 1, in which case it's assumed that
all reps start with the same density of parasitoids.}

\item{virus_attract}{Effect of virus infection on alate alighting.
Sampling weights for virus-infectious plants is \code{virus_attract},
compared to empty (no virus or \emph{Pseudomonas}) plants whose weight is 1.
Thus, when \code{virus_attract > 1}, alates are attracted to virus-infectious
plants, while \code{virus_attract < 1} causes them to be repelled by
virus-infectious plants.
Values must be \verb{> 0}.}

\item{pseudo_repel}{Effect of \emph{Pseudomonas} infection on alate alighting.
Sampling weights for \emph{Pseudomonas}-containing plants is
\code{1 / pseudo_repel} (note difference from \code{virus_attract}, hence the
different names!), compared to empty (no virus or \emph{Pseudomonas})
plants whose weight is 1.
Thus, when \code{pseudo_repel > 1}, alates are repelled by
\emph{Pseudomonas}-containing plants,
while \code{pseudo_repel < 1} causes them to be attracted to
\emph{Pseudomonas}-containing plants.
Values must be \verb{> 0}.}

\item{epsilon}{Effect of virus infection on alate acceptance.
Values \verb{> 1} cause alates to be more likely to stay and feed
(indefinitely) on virus-infected plants,
while values \verb{< 1} cause them to be less likely to stay and feed on
virus-infected plants.
Values must be \verb{> 0}, and \code{epsilon * w} must be \verb{< 1}.}

\item{p_load_alate}{Single numeric indicating the probability that an
uninoculated alate is loaded with a virus if it interacts with an
inoculated plant.}

\item{p_load_plant}{Single numeric indicating the probability that an
uninoculated plant is loaded with a virus if it interacts with an
inoculated alate.}

\item{total_exp_days}{Single integer indicating the number of days required
for a plant to transition from exposed (inoculated with virus but
not able to pass it on) to infectious (able to infect other plants).
A value of \code{0} means that an alate inoculating a plant causes that
plant to be infectious the same day.
Because this is not realistic and computationally troublesome,
only values \verb{>1} are allowed.
Defaults to \code{7}, which is based on the paper "Cucumber mosaic virus
isolates seedborne in \emph{Phaseolus vulgaris}: serology, host-pathogen
relationships, and seed transmission" (Davis & Hampton, 1986).}

\item{w}{Probability that an alate accepts a plant, meaning that
it stays to feed on it indefinitely.
Must be \verb{> 1e-4} and \verb{< 1}.
The lower limit is because a very small value of \code{w} causes alates to
fly to so many plants that it becomes computationally problematic.
This is the same reason that I check for (and return and error) if
\code{w*epsilon < 1e-4}.
Defaults to \code{0.2}.}

\item{radius}{Max distance that alates will travel between plants.
Defaults to \code{7.336451}, which is based on previous work.
See "Radius" section below for details.}

\item{wasp_plant_attract}{Relative attractiveness of plants to wasps.
This affects the proportion of wasps that immigrate from the dispersal
pool to each plant.
It doesn't change the number of wasps that leave plants.
This must be \code{NULL} or a matrix the same dimensions as each slice
of \code{landscapes}.
If \code{NULL}, all plants are equally attractive to wasps.
If a matrix is provided, then the values are divided by their sum
(to make it sum to 1), then those values are used as the proportion of
wasps from the dispersal pool that immigrate to each plant.
Defaults to \code{NULL}.}

\item{summ}{Single string to indicate how to summarize output.
If \code{summ == "none"}, then no summarizing is done, so output is separate
by plant.
If \code{summ == "pseudo"}, then output is summarized by whether plants
contain \emph{Pseudomonas}.
If \code{summ == "time"}, then output is summarized across all plants, so
will only be separated by time and rep.
If \code{summ == "all"}, then output is summarized across all plants and
time points, so will only be separated by rep.
See "Summarizing" section below for details on output columns.
Defaults to \code{"none"}.}

\item{infect_time_n}{Single integer specifying the number of plants
to use when calculating the \code{infect_time} column when \code{summ == "all"}.
Ignored when \code{summ != "all"} except for error checking.
Note that since this is coerced to an unsigned integer, using
a negative number here could cause an error to occur.
Defaults to \code{0}, which results in the total number of plants
(i.e., \code{prod(dim(landscapes)[1:2])}) being used.}

\item{infect_stop}{Single logical for whether to stop simulations
when all plants are infected with virus.
Defaults to \code{TRUE}.}

\item{out_pseudo}{Single logical for whether to include \emph{Pseudomonas}
presence in output. Ignored if \code{summ != "none"}.
Defaults to \code{FALSE}.}

\item{show_progress}{Single logical for whether to show progress bar.
Defaults to \code{FALSE}.}

\item{n_threads}{Single integer for the number of threads to use.
Ignored if \code{dim(landscapes)[3] == 1}.
Defaults to \code{0L}, in which case it takes the value from
\code{options("mc.cores")} if assigned, and \code{1L} if not assigned.}
}
\value{
A tibble with columns following the description in the
"Summarizing" section.
}
\description{
Simulation plant landscapes with virus spread.
}
\section{Radius}{
From "The Role of Aphid Behaviour in the Epidemiology of Potato Virus Y:
a Simulation Study" by Thomas Nemecek (1993; p. 72), dispersal distances
follow a Weibull distribution with shape = 0.6569 and scale = 9.613.

The default for the \code{radius} argument uses the median of this
distribution.
I'm dividing by 0.75 to convert from meters to plant locations that are
0.75 meters apart (typical spacing for pea):
\code{radius = qweibull(0.5, 0.6569, 9.613) / 0.75}.
}

\section{Summarizing}{
If \code{summ == "none"}, \code{n_x * n_y} rows are output for each rep and time point.
The columns are...
\enumerate{
\item \code{rep}: repetition number
\item \code{time}: time point
\item \code{x}: plant x coordinate
\item \code{y}: plant y coordinate
\item \code{pseudo}: plant contains \emph{Pseudomonas} (0 or 1); note: this column isn't
included if \code{out_pseudo = FALSE}
\item \code{virus}: plant infectious with virus (0 or 1)
\item \code{aphids}: aphid (non-winged) density
\item \code{alates}: alate density
\item \code{parasitized}: parasitized aphid density
\item \code{mummies}: mummy density
\item \code{wasps}: total parasitoid density (\code{x} and \code{y} for this row will be 0)
}

If \code{summ == "pseudo"}, two rows are output for each rep and time point.
The columns are...
\enumerate{
\item \code{rep}: repetition number
\item \code{time}: time point
\item \code{pseudo}: plant contains \emph{Pseudomonas} (0 or 1)
\item \code{n}: total plants of this type
\item \code{virus}: number of plants infectious with virus
\item \code{aphids}: total aphid (non-winged) density summed across all plants of
this type
\item \code{alates}: total alate density summed across all plants of this type
\item \code{parasitized}: total parasitized aphid density summed across all plants
of this type
\item \code{mummies}: total mummy density summed across all plants of this type
10.\code{wasps}:  total parasitoid density (\code{type} for this row will be \code{2})
}

If \code{summ == "time"}, one row is output per rep and time point.
The columns are...
\enumerate{
\item \code{rep}: repetition number
\item \code{time}: time point
\item \code{virus}: number of plants infectious with virus
\item \code{aphids}: total aphid (non-winged) density summed across all plants
\item \code{alates}: total alate density summed across all plants
\item \code{parasitized}: total parasitized aphid density summed across all plants
\item \code{mummies}: total mummy density summed across all plants
\item \code{wasps} total parasitoid density
}

If \code{summ == "all"}, one row is output per rep.
The columns are...
\enumerate{
\item \code{rep}: repetition number
\item \code{p_alates}: mean total alates / total aphids
\item \code{log_aphids}: mean log(aphids+1)
\item \code{aphids}: mean aphids
\item \code{log_alates}: mean log(alates+1)
\item \code{alates}: mean alates
\item \code{log_alates}: mean log(parasitized aphids+1)
\item \code{alates}: mean parasitized aphids
\item \code{log_mummies}: mean log(mummies+1)
\item \code{mummies}: mean mummies
\item \code{log_wasps}: mean log(wasps+1)
\item \code{wasps}: mean wasps
\item \code{infect_time}: time it took to have \code{infect_time_n} plants infected
\item \code{outbreak_size}: maximum number of plants infected with virus
}
}

