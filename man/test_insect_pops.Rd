% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{test_insect_pops}
\alias{test_insect_pops}
\title{Test population dynamics for insects for a set of parameters.}
\usage{
test_insect_pops(
  max_t,
  A0,
  W0,
  P0,
  B,
  a,
  h,
  k,
  s,
  surv_j = NA_real_,
  surv_a = NA_real_,
  recruit = NA_real_,
  fecund = NA_real_,
  K = NA_real_,
  alate_0 = NA_real_,
  alate_1 = NA_real_
)
}
\arguments{
\item{max_t}{Single integer giving the maximum time the simulations run.}

\item{A0}{Numeric matrix indicating the starting aphid (non-winged) population
density for each plant.
To indicate separate densities for each plant, the matrix should
have the same number of rows and columns as \code{landscapes}.
The matrix can also be 1x1, in which case it's assumed that all plants
start with the same density of aphids.}

\item{W0}{Numeric matrix indicating the starting winged aphid population
density for each plant.
To indicate separate densities for each plant, the matrix should
have the same number of rows and columns as \code{landscapes}.
The matrix can also be 1x1, in which case it's assumed that all plants
start with the same density of winged aphids.}

\item{P0}{Numeric matrix indicating the starting predator population
density for each plant.
To indicate separate densities for each plant, the matrix should
have the same number of rows and columns as \code{landscapes}.
The matrix can also be 1x1, in which case it's assumed that all plants
start with the same density of predators.}

\item{B}{Single numeric indicating the effect of \emph{Pseudomonas} on aphid
population growth.}

\item{a}{Single numeric indicating the natural enemy attack rate.}

\item{h}{Single numeric indicating the natural enemy handling time.}

\item{k}{Single numeric indicating the natural enemy aggregation parameter.}

\item{s}{Single numeric indicating the natural enemy daily survival.}

\item{surv_j}{Single numeric indicating aphid juvenile survival.
Defaults to \code{NA}, which results in \code{pop_info$surv_j} being used.
This is from a previous study.}

\item{surv_a}{Single numeric indicating aphid adult survival.
Defaults to \code{NA}, which results in \code{pop_info$surv_a} being used.
This is from a previous study.}

\item{recruit}{Single numeric indicating aphid recruitment.
Defaults to \code{NA}, which results in \code{pop_info$recruit} being used.
This is from a previous study.}

\item{fecund}{Single numeric indicating aphid fecundity.
Defaults to \code{NA}, which results in \code{pop_info$fecund} being used.
This is from a previous study.}

\item{K}{Single numeric indicating the carrying capacity (not including
natural enemies or \emph{Pseudomonas}) of the aphid population.
Defaults to \code{NA}, which results in \code{pop_info$K} being used.
This is from a previous study.}

\item{alate_0}{Single numeric.
The proportion of winged offspring from apterous aphids is
\code{inv_logit(alate_0 + alate_1 * z)} where \code{z} is the total number of
aphids on that plant.
Defaults to \code{NA}, which results in \code{pop_info$alate_0} being used.
This is from a previous study.}

\item{alate_1}{Single numeric affecting how strongly aphid density
influences alate production. See \code{alate_0} above for the equation.
Defaults to \code{NA}, which results in \code{pop_info$alate_1} being used.
This is from a previous study.}
}
\description{
This function is just for one plant, so the probability that an
alate leaves the plant (argument \code{fly_p} in \code{\link[=make_insect_ptr]{make_insect_ptr()}}) is always 1.
}
